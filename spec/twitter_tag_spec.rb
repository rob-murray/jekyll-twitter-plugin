
# frozen_string_literal: true
RSpec.describe TwitterJekyll::TwitterTag do
  describe "output" do
    context "with cache" do
      it "renders response from cache"
    end

    context "without cache" do
      it "renders response from api"
    end
  end
end
